server:
  port: 9000

spring:
  application:
    name: mes-gateway
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  cloud:
    gateway:
      routes:
        # 认证服务
        - id: mes-auth
          uri: lb://mes-auth
          predicates:
            - Path=/api/auth/**
          filters:
            - StripPrefix=1

        # 系统服务
        - id: mes-system
          uri: lb://mes-system
          predicates:
            - Path=/api/system/**
          filters:
            - StripPrefix=1
  data:
    redis:
      port: 6379

# 安全配置 - 白名单
ignore:
  whites:
    - /auth/login
    - /auth/register
    - /auth/captcha

# Token 配置
token:
  header: Authorization
  secret: mescloudmescloudmescloudmescloudmescloud
  expireTime: 120
  prefix: "Bearer "

# 日志
logging:
  level:
    com.mes: debug
    org.springframework.cloud.gateway: debug
