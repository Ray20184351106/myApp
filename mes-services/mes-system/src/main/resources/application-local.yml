# 本地开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mes_system?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8&useUnicode=true
    username: root
    password: mysql
  data:
    redis:
      host: localhost
