# Docker环境配置
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/mes_auth?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8&useUnicode=true
  data:
    redis:
      host: redis
