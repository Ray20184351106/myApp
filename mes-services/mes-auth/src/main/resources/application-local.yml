# 本地开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mes_auth?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: mysql
  data:
    redis:
      host: localhost
